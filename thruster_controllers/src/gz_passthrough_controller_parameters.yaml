gz_passthrough_controller:
  thruster:
    type: string
    default_value: ""
    read_only: true
    description: "The name of the thruster"
    validation:
      not_empty<>: null

  topic:
    type: string
    default_value: ""
    read_only: true
    description: "The Gazebo topic to publish the thruster command to"
    validation:
      not_empty<>: null
