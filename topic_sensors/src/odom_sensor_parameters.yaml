odom_sensor:
  topic:
    type: string
    default_value: "/odom"
    description: "The name of the odometry topic to subscribe to."

  prefix:
    type: string
    default_value: ""
    description: "The prefix to add to the state interface."
